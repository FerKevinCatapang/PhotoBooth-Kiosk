{
  "name": "photo-booth-kiosk",
  "version": "1.0.0",
  "private": true,
  "description": "Photo Booth Kiosk – Capacitor Android wrapper",
  "scripts": {
    "sync":      "cap sync android",
    "open":      "cap open android",
    "build:apk": "cd android && .\\gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
